<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mif.prunus.front.board.mapper.BoardMyBatisMapper">
	<select id="selectBoardList" parameterType="mif.prunus.front.board.vo.SearchDataVO" resultType="HashMap">
		select * from board
	</select>
</mapper>